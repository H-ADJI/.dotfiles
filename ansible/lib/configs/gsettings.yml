- name: setup gsettings ( GNOME SETTINGS )
  hosts: localhost
  become: true
  tasks:
    - name: Download Docker installation script
      shell: |
        bash {{ home_dir }}/.dotfiles/gsettings/workspaces.sh
